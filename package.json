{
  "name": "nook-backend",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "build": "node --check src/server.mjs && node --check src/gateway/anthropic.mjs && node --check src/gateway/payload.mjs && node --check nocturne-client.mjs",
    "test": "node --test"
  }
}
